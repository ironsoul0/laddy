{
  "name": "laddy",
  "private": true,
  "devDependencies": {
    "lerna": "^3.19.0"
  },
  "scripts": {
    "bootstrap": "lerna bootstrap --hoist",
    "start": "lerna run start --stream",
    "client": "lerna run start --scope=@laddy/client --stream",
    "server": "lerna run start --scope=@laddy/server --stream",
    "fill-data": "lerna run fill-data --scope=@laddy/server --stream"
  }
}
